<%= tabs %>
<h1>Listando curso</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @majors.each do |major| %>
    <tr>
      <td><%=major.name%></td>
      <td><%= link_to 'Editar Curso', edit_major_path(major) %></td>
      <td><%= link_to 'Ver Grades', major_syllabuses_path(major) %></td>
      <td><%= link_to 'Nova Grade', new_major_syllabus_path(major) %></td>
      <td><%= link_to 'Exluir Curso', major, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Major', new_major_path %>
